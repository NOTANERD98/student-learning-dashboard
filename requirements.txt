# Python dependencies for the Streamlit dashboard.
# Pinned to a known-good, mutually compatible set with Linux (manylinux) wheels
# for Python 3.12 — guarantees a fast, reproducible build on Streamlit Cloud.
streamlit==1.41.1
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
matplotlib==3.9.2
seaborn==0.13.2
